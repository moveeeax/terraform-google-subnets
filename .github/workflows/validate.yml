name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: opentofu/setup-opentofu@v1
        with:
          tofu_wrapper: false

      - name: fmt
        run: tofu fmt -check -recursive

      - name: init
        run: tofu init -backend=false

      - name: validate
        run: tofu validate

      # Runs against a mocked provider, so it needs no credentials and makes no
      # API calls.
      - name: test
        run: tofu test

      - name: validate examples
        run: |
          set -euo pipefail
          for dir in examples/*/; do
            echo "==> $dir"
            (cd "$dir" && tofu init -backend=false && tofu validate)
          done
